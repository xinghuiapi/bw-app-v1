targets:
  $default:
    builders:
      json_serializable:
        generate_for:
          include:
            - lib/models/**/*.dart
          exclude:
            - lib/widgets/**
            - test/**
